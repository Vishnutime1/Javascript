mycode = {
    js : "Javascript",
    py : "Python",
    cpp : "C++"
}

// for (const key in mycode) {
   
//         console.log(`${key}${mycode[key]} is shortcut for ${mycode[key]}`);
// }

const myarr = ["js","py","ts"]

for (const key in myarr) {
   console.log(`This  ${key} is key for ${myarr[key]}`);
}